这份Day06的填空版笔记已经整理好了。整体结构和需求说明已保留，具体的知识点解析、代码实现和表格内容均已留空，方便上午的课程中带着大家边思考边推敲。

------

# Python高级与数据分析 Day06 - 正则表达式与 MySQL 数据库

### 一. 今日目标

Plaintext

```
1. 能够知道正则表达式的作用
2. 能够使用 re 模块匹配单个字符
3. 能够使用 re 模块匹配多个字符
4. 能够使用 re 模块匹配指定字符串开头或结尾
5. 能够使用 re 模块提取分组数据
6. 能够独立安装 MySQL 数据库
7. 能够独立创建数据库和表
8. 能够独立增、改、删数据记录
9. 掌握数据库单表查询语句
10. 掌握数据库多表查询语句
11. 能够说出 DDL、DML、DQL 的区别
12. 能够知道正则表达式在 RAG 文档清洗中的简单应用
```

> 说明：前 10 条来自今日反馈文件，是本日必须掌握的核心目标；第 11、12 条是为了帮助理解 MySQL 和 AI 大模型方向场景补充的重点。

### 二. 正则表达式概述

#### 1. [掌握]正则表达式的作用

- **正则表达式**：用一套规则去找一类字符串
- **普通字符串查找**：固定值，比如“123”比如“abc”比如“AI”
- **正则表达式查找**：查符合一类规则的数据，例如日期，手机，邮箱，订单号

#### 2. [理解]为什么要学习正则表达式

- AI开发：清洗数据，训练模型，RAG，规范输出格式，脱敏
- 不是Python独有的，c，java

#### 3. [互动提问]

- **问题 1**：如果只是判断一句话中有没有固定词 `"Python"`，必须用正则吗？

  - **答案**：不必须，用in或者find

- **问题 2**：如果要从一段文本中提取所有手机号，适合用正则吗？

  - **答案**：适合，1[\d]{10}

- **问题 3**：正则表达式的核心作用是什么？

  - **答案**：匹配校验清洗一类字符串

### 三. re 模块基础

#### 1. [掌握]re 模块使用三步

> Python
>
> ```
> import re
> 
> text = "abc"
> 
> result = re.match(partten:)
> 
> result.group()
> ```

#### 2. [掌握]三种常用匹配方式

- **`re.match()`**：开头，没找到停止，返回None

- **`re.search()`**：整个字符串，返回第一个

- **`re.findall()`**：整个字符串，列表返回所有结果

- **记忆口诀**：

  > Python
  >
  > ```python
  > """
  > 需求：查看正则表达式match的用法，以及结果
  > 如果开头匹配到是什么结果，匹配不到是什么结果
  > 
  > 步骤：
  > 1.导包
  > 2.匹配，写表达式
  > 3.获取结果
  > 
  > """
  > 
  > # 1.导包
  > import re
  > 
  > # 2.匹配，写表达式
  > heima = "12345黑马程序员毕业的都是黑马"
  > 
  > result = re.match(pattern="黑马", string=heima)
  > 
  > # 3.获取结果
  > if result:
  >     print(f"匹配成功：{result.group()}")
  > else:
  >     print("匹配失败")
  > 
  > """
  > 需求：查看正则表达式match的用法，以及结果
  > 如果开头匹配到是什么结果，匹配不到是什么结果
  > 
  > 步骤：
  > 1.导包
  > 2.匹配，写表达式
  > 3.获取结果
  > 
  > """
  > 
  > # 1.导包
  > import re
  > 
  > # 2.匹配，写表达式
  > heima = "12345黑马程序员毕业的都是黑马"
  > 
  > result = re.search(pattern="黑马", string=heima)
  > 
  > # 3.获取结果
  > if result:
  >     print(f"匹配成功：{result.group()}")
  > else:
  >     print("匹配失败")
  > 
  > """
  > 需求：查看正则表达式match的用法，以及结果
  > 如果开头匹配到是什么结果，匹配不到是什么结果
  > 
  > 步骤：
  > 1.导包
  > 2.匹配，写表达式
  > 3.获取结果
  > 
  > """
  > 
  > # 1.导包
  > import re
  > 
  > # 2.匹配，写表达式
  > heima = "12345黑马程序员都是黑马中的黑马"
  > 
  > result = re.findall(pattern="黑马", string=heima)
  > 
  > # 3.获取结果
  > if result:
  >     print(f"匹配成功：{result}")
  > else:
  >     print("匹配失败")
  > ```

#### 3. [互动提问]

- **问题 1**：`match` 和 `search` 最大区别是什么？
  - **答案**：match从头开始，没有就不再找了，search接着找，找整个字符串
- **问题 2**：如果要提取一段文本中的所有手机号，用哪个函数更合适？
  - **答案**：用findall
- **问题 3**：`findall()` 常见返回结果是什么类型？
  - **答案**：[]

### 四. 正则表达式常用规则

#### 1. [掌握]匹配单个字符

| **规则** | **含义**          |
| -------- | ----------------- |
| `.`      | 任意字符除了\n    |
| `[abc]`  | 查找a,b,c中的一个 |
| `[^abc]` | 查找非a,b,c的字符 |
| `\d`     | 找数字            |
| `\D`     | 非数字            |
| `\s`     | 匹配空白字符      |
| `\S`     | 非空白            |
| `\w`     | 非特殊字符        |
| `\W`     | 特殊字符          |

> Python
>
> ```
> 
> ```

#### 2. [掌握]匹配多个字符
w
| **规则** | **含义**     |
| -------- | ------------ |
| `*`      | 0-无限都匹配 |
| `+`      | 1-无限       |
| `?`      | 0或1         |
| `{m}`    | 匹配m个      |
| `{m,n}`  | 匹配m到n个   |

- **重点**：

  > Python
  >
  > ```
  > 
  > ```

#### 3. [掌握]匹配指定字符串开头或结尾

| **规则** | **含义** |
| -------- | -------- |
| `^`      | 开头     |
| `$`      | 结尾     |

- **提取数据**：

- **校验数据**：

  > Python
  >
  > ```
  > 
  > ```

#### 4. [掌握]提取分组数据

- **分组符号**：()

- **作用**：

  > Python
  >
  > ```
  > 
  > ```

#### 5. [理解]贪婪和非贪婪模式

- **贪婪模式**：尽可能多的匹配

- **非贪婪模式**：尽可能少的匹配

- **常见写法**：

  - 贪婪：(.*)
  - 非贪婪：(.*?)

  > Python
  >
  > ```
  > 
  > ```

#### 6. [互动提问]

- **问题 1**：`\d{11}` 表示什么？
  - **答案**：匹配11个数字
- **问题 2**：为什么校验手机号时要写 `^1\d{10}$`，而不是只写 `\d+`？
  - **答案**：限制开头结尾，限制长度
- **问题 3**：分组有什么用？
  - **答案**：按部分匹配，还能重复利用
- **问题 4**：如果正则匹配结果太长，应该优先检查什么？
  - **答案**：检查贪婪或非贪婪

### 五. 正则表达式综合案例：RAG 文档清洗

#### 1. [掌握]案例需求

给定一段原始文档文本，完成：

1. 提取手机号。
2. 删除页码。
3. 删除内部资料标记。
4. 删除页脚。
5. 压缩连续多个空行。

#### 2. [掌握]代码实现

> Python
>
> ```
> 
> ```

#### 3. [互动提问]

- **问题 1**：RAG 文档清洗中，为什么要删除页码、页脚、重复空行？
  - **答案**：
- **问题 2**：`re.sub()` 的作用是什么？
  - **答案**：

### 六. MySQL 数据库基础

#### 1. [掌握]数据库、数据表、字段

- **数据库**：存储和管理数据的系统
- **数据表**：数据库中保存数据的结构
- **字段**：表中的列，用来描述记录属性
- **记录**：表中的一行数据

#### 2. [掌握]SQL 分类

| **分类** | **全称**                   | **作用**           | **常见关键字**      |
| -------- | -------------------------- | ------------------ | ------------------- |
| DDL      | Data Definition Language   | 定义数据库和表结构 | create，drop，alter |
| DML      | Data Manipulation Language |                    |                     |
| DQL      | Data Query Language        |                    |                     |
| DCL      | Data Control Language      |                    |                     |

- **记忆口诀**：
  - DDL：
  - DML：
  - DQL：

#### 3. [互动提问]

- **问题 1**：MySQL 和数据库是一个东西吗？
  - **答案**：不是，数据库是大类，mysql是其中的一个关系型数据库管理系统
- **问题 2**：`CREATE TABLE` 属于 DDL、DML、DQL 中哪一类？
  - **答案**：DDL，因为他在创建表的结构
- **问题 3**：`SELECT` 属于哪一类 SQL？
  - **答案**：DQL，查询

### 七. DDL：创建数据库和表

#### 1. [掌握]创建电商示例数据库和表

下面的 `category` 和 `product` 是今天课堂统一使用的电商示例表，需要同学们自己创建。后面的单表查询和多表查询都会基于这两张表展开。

> SQL
>
> ```
> -- 创建一个今天练习专用的数据库。
> -- DEFAULT CHARSET utf8mb4 用来支持中文，避免商品名、分类名出现乱码。
> CREATE DATABASE day06_shop DEFAULT CHARSET utf8mb4;
> 
> -- 切换到 day06_shop 数据库。
> -- 后面创建表和插入数据都会在这个库里完成。
> USE day06_shop;
> 
> -- 创建分类表 category。
> -- 作用：保存商品分类，例如“手机”“电脑配件”“办公用品”。
> CREATE TABLE category (
>     -- 分类 id，主键，自增。
>     -- 主键的作用是唯一标识一条分类记录。
>     id INT PRIMARY KEY AUTO_INCREMENT,
> 
>     -- 分类名称，最多 50 个字符，不能为空。
>     name VARCHAR(50) NOT NULL
> );
> 
> -- 创建商品表 product。
> -- 作用：保存具体商品信息。
> CREATE TABLE product (
>     -- 商品 id，主键，自增。
>     id INT PRIMARY KEY AUTO_INCREMENT,
> 
>     -- 商品名称，不能为空。
>     name VARCHAR(100) NOT NULL,
> 
>     -- 商品价格，保留两位小数。
>     -- 金额类数据建议使用 DECIMAL，结果更精确。
>     price DECIMAL(10, 2) NOT NULL,
> 
>     -- 商品库存，表示当前剩余数量。
>     stock INT NOT NULL,
> 
>     -- 商品所属分类 id。
>     -- 后面做多表查询时，会用它关联 category 表的 id。
>     category_id INT
> );
> 
> 
> -- 向分类表插入 3 条分类数据。
> -- 后面的商品会通过 category_id 归属到这些分类中。
> INSERT INTO category(name) VALUES
> ('手机'),
> ('电脑配件'),
> ('办公用品');
> 
> -- 向商品表插入 5 条商品数据。
> -- 字段顺序是：商品名称、商品价格、库存数量、所属分类 id。
> -- category_id 为 1 表示手机，为 2 表示电脑配件，为 3 表示办公用品。
> INSERT INTO product(name, price, stock, category_id) VALUES
> ('iPhone 15', 5999.00, 100, 1),
> ('机械键盘', 299.00, 50, 2),
> ('无线鼠标', 99.00, 80, 2),
> ('笔记本支架', 59.00, 120, 3),
> ('安卓充电线', 29.90, 200, 1);
> ```

#### 2. [互动提问]

- **问题 1**：创建表之前为什么要先 `USE day06_shop`？
  - **答案**：因为要指定后续创建表的数据库
- **问题 2**：`category_id` 的作用是什么？
  - **答案**：商品分类的id

### 八. DML：插入、修改、删除数据

#### 1. [掌握]插入示例数据

> SQL
>
> ```sql
> insert into values (),()
> -- 向分类表插入 3 条分类数据。
> -- 后面的商品会通过 category_id 归属到这些分类中。
> INSERT INTO category(name) VALUES
> ('手机'),
> ('电脑配件'),
> ('办公用品');
> 
> -- 向商品表插入 5 条商品数据。
> -- 字段顺序是：商品名称、商品价格、库存数量、所属分类 id。
> -- category_id 为 1 表示手机，为 2 表示电脑配件，为 3 表示办公用品。
> INSERT INTO product(name, price, stock, category_id) VALUES
> ('iPhone 15', 5999.00, 100, 1),
> ('机械键盘', 299.00, 50, 2),
> ('无线鼠标', 99.00, 80, 2),
> ('笔记本支架', 59.00, 120, 3),
> ('安卓充电线', 29.90, 200, 1);
> ```

#### 2. [掌握]修改数据

> SQL
>
> ```
> update set x=x
> -- 把“机械键盘”的价格改成 279。
> -- WHERE name = '机械键盘' 表示只修改这一条商品。
> -- 如果没有 WHERE，可能会把整张 product 表的商品价格都改掉。
> UPDATE product SET price = 279.00 WHERE name = '机械键盘';
> ```

#### 3. [掌握]删除数据

> SQL
>
> ```
> -- 删除“无线鼠标”这条商品数据。
> -- WHERE name = '无线鼠标' 表示只删除指定商品。
> -- 如果没有 WHERE，可能会删除整张 product 表的数据。
> DELETE FROM product WHERE name = '无线鼠标';
> ```

#### 4. [互动提问]

- **问题 1**：`UPDATE product SET price = 99;` 错在哪里？
  - **答案**：少了where比如name 或者id等于某个值
- **问题 2**：删除数据时为什么也要写 `WHERE`？
  - **答案**：不要全删了

### 九. DQL：单表查询

#### 1. [掌握]基础查询

> SQL
>
> ```
> select * from 表名;
> ```

#### 2. [掌握]条件查询

> SQL
>
> ```
> select * from 表名  where ;
> ```

#### 3. [掌握]排序查询

> SQL
>
> ```
> order by 1,2
> ```

#### 4. [掌握]聚合函数

> SQL
>
> ```
> count avg max min sum
> ```

#### 5. [掌握]分组查询

> SQL
>
> ```
> group by 1,2
> ```

#### 6. [掌握]LIMIT 查询

> SQL
>
> ```
> 从m+1开始，n行数据  如果m=0 可以不写,默认从第一行开始
> ```

#### 7. [理解]SQL 逻辑执行顺序

常见逻辑顺序：

> Plaintext
>
> ```
> FROM-WHERE=GROUP BY-聚合函数-HAVING-SELECT-ORDER BY-LIMIT
> ```

- **面试点**：

#### 8. [互动提问]

- **问题 1**：查询价格大于 100 的商品，用哪个关键字？
  - **答案**：WHERE
- **问题 2**：查询每个分类下商品数量，需要使用什么？
  - **答案**：GROUP BY COUNT
- **问题 3**：查询库存最低的前 3 个商品，为什么要先 `ORDER BY stock ASC`？
  - **答案**：因为要先排好顺序，才能取出

### 十. DQL：多表查询

#### 1. [理解]为什么需要多表查询

* 真实业务中，不会把所有数据都塞进一张表。
* 商品表保存商品自身信息，分类表保存分类信息。
* 商品表通过 `category_id` 和分类表的 `id` 建立关系。
* **核心思想**：按关系把多张表临时拼成一张结果表，再按照单表查询思路处理。

#### 2. [掌握]内连接

> SQL
>
> ```
> 两张表匹配的部分，不匹配的不返回
> ```

- **内连接特点**：

#### 3. [掌握]左连接

> SQL
>
> ```
> 左表有的都展示，右表没有的填充null
> ```

- **左连接特点**：

#### 4. [掌握]子查询

> SQL
>
> ```
> 两个select语句嵌套子查询语句用()包裹也是完整的语句，子语句限制性，辅助查询
> ```

- **核心理解**：

#### 5. [互动提问]

- **问题 1**：为什么商品表不直接重复保存分类名称？
  - **答案**：避免数据重复，方便维护
- **问题 2**：多表查询中 `ON p.category_id = c.id` 的作用是什么？
  - **答案**：找出p表的cid和c表id相同，指定连接条件
- **问题 3**：如果多表查询漏写连接条件，会出现什么问题？
  - **答案**：笛卡尔积，非常恐怖，左行乘以右边行
- **问题 4**：内连接和左连接的区别是什么？
  - **答案**：内连接匹配上才展示，左连接，左边全部，右边没匹配的填充null

### 十一. 今日综合练习

#### 1. [练习]正则提取手机号

> Python
>
> ```
> 
> ```

#### 2. [练习]正则提取订单号

> Python
>
> ```
> 
> ```

#### 3. [练习]MySQL 查询商品和分类

> SQL
>
> ```
> 
> ```

### 十二. 今日总结

#### 1. 按反馈目标回收

- **能够知道正则表达式的作用**：

- **能够使用 re 模块匹配单个字符**：

- **能够使用 re 模块匹配多个字符**：

- **能够使用 re 模块匹配指定字符串开头或结尾**：

- **能够使用 re 模块提取分组数据**：

- **能够独立安装 MySQL 数据库**：

- **能够独立创建数据库和表**：

- **能够独立增、改、删数据记录**：

  insert into 

  update 

  delete 注意条件，例如where

- **掌握数据库单表查询语句**：

  select *

  where 

  group by

  avg()

  count()

  sum()

  limit m,n 

  m+1行开始，取n行，m+1不能超过真实行数，如果m为0可以省略

- **掌握数据库多表查询语句**：

  不能直接连没有限定条件，会发生笛卡尔积

  on 

  inner join

  left join

#### 2. 易错点：错误与正确对照

| **易错点**   | **错误写法 / 错误理解** | **正确写法 / 正确理解** |
| ------------ | ----------------------- | ----------------------- |
| 正则字符串   |                         |                         |
| `match` 用法 |                         |                         |
| 手机号校验   |                         |                         |
| 量词作用对象 |                         |                         |
| 贪婪匹配     |                         |                         |
| 分组提取     |                         |                         |
| 建表位置     |                         |                         |
| 修改数据     |                         |                         |
| 删除数据     |                         |                         |
| 多表查询     |                         |                         |
| 字段同名     |                         |                         |
| 分组统计     |                         |                         |

#### 3. 今日必须记住的几句话

------