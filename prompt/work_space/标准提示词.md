# 任务目标：基于 SPEC-C 与 RTCOE 融合框架生成金融文本分类项目 (project3.py)

---

## 1. Specification (S - 项目规格与技术栈)
- **目标文件名**：`project3.py`
- **文件保存绝对路径**：`c:\Users\YuEth\Desktop\AI综合学习方案\技术路线\Machine-learning-learning\prompt\work_space\`
- **技术栈与依赖库**：`python-dotenv`、`openai`、`json`、`os`。
- **结构参照对象**：同目录下的 `project2.py`。

---

## 2. Execution Pipeline (P - 代码架构与执行流程)
请在生成的 `project3.py` 中严格按照以下 5 个模块构建代码：
1. **环境与依赖加载**：调用 `load_dotenv()`，配置包含降级默认值的 `OpenAI` 客户端。
2. **提示词构建 (`init_prompts`)**：将下文 `# 3. Exact Logic` 中的 RTCOE 系统提示词与 Few-Shot 范例构造为上下文历史字典 `{'pre_history': [...]}`。
3. **模型推理调用 (`model_chat`)**：封装大模型 Chat Completions 接口，传入待分类句子与 `pre_history`。
4. **后处理清洗 (`clean_response`)**：清理模型输出，匹配合法分类标签（若有异常进行安全兜底）。
5. **主逻辑与验证输出 (`__main__`)**：遍历测试句子列表，调用推理与后处理，格式化打印预测结果。

---

## 3. Exact Logic (E - 嵌入代码的 RTCOE 核心提示词与数据集)

### (1) 嵌入代码的 RTCOE 系统提示词与规则 (<rtcoe_prompt>)
```text
# Role (角色身份)
你是一名资深金融风控与文本分析专家，具备深厚的金融知识。

# Context (背景上下文)
你需要处理来自金融资讯平台的短文本数据，准确归类以支持自动风控与舆情监控。

# Task (核心任务)
分析传入的金融文本，精确归类到以下四种预设类别之一：
- 新闻报道
- 财务报告
- 公司公告
- 分析师报告

# Constraints & Rules (规则与约束)
1. 兜底逻辑：若文本不属于以上任何一类或语义模糊无法判定，必须且只能返回：'不清楚类型'。
2. 输出限制：必须且只能输出最终的类别名称字符串，严禁包含任何标点符号、解释性文字、引导词或 Markdown 标签。
3. 防幻觉约束：严格依据文本本身内容判定，严禁主观推测文本未提及的隐藏含义。

# Output Format (输出格式)
新闻报道 | 财务报告 | 公司公告 | 分析师报告 | 不清楚类型

# Examples (少样本示例)
[输入]: """今日，股市经历了一轮震荡，受到宏观经济数据和全球贸易紧张局势的影响。投资者密切关注美联储可能的政策调整，以适应市场的不确定性。"""
[输出]: 新闻报道

[输入]: """本公司年度财务报告显示，去年公司实现了稳步增长的盈利，同时资产负债表呈现强劲的状况。经济环境的稳定和管理层的有效战略执行为公司的健康发展奠定了基础。"""
[输出]: 财务报告

[输入]: """本公司高兴地宣布成功完成最新一轮并购交易，收购了一家在人工智能领域领先的公司。这一战略举措将有助于扩大我们的业务领域，提高市场竞争力"""
[输出]: 公司公告

[输入]: """最新的行业分析报告指出，科技公司的创新将成为未来增长的主要推动力。云计算、人工智能和数字化转型被认为是引领行业发展的关键因素，投资者应关注这些趋势"""
[输出]: 分析师报告
```

### (2) 嵌入代码的待测试数据集 (`sentences`)
```python
sentences = [
    "今日，央行发布公告宣布降低利率，以刺激经济增长。这一降息举措将影响贷款利率，并在未来几个季度内对金融市场产生影响。",
    "ABC公司今日发布公告称，已成功完成对XYZ公司股权的收购交易。本次交易是ABC公司在扩大业务范围、加强市场竞争力方面的重要举措。据悉，此次收购将进一步巩固ABC公司在行业中的地位，并为未来业务发展提供更广阔的发展空间。详情请见公司官方网站公告栏",
    "公司资产负债表显示，公司偿债能力强劲，现金流充足，为未来投资和扩张提供了坚实的财务基础。",
    "最新的分析报告指出，可再生能源行业预计将在未来几年经历持续增长，投资者应该关注这一领域的投资机会"
]
```

---

## 4. Coding Constraints (C - 编码规范与硬性约束)
- **环境变量容错降级**：
  在 `os.environ.get()` 获取 API 参数时，必须设置如下默认兜底值（防止无 `.env` 文件时报错）：
  - `DASHSCOPE_BASE_URL`: `'https://ws-tb5h1ovj9i2pokh3.cn-beijing.maas.aliyuncs.com/compatible-mode/v1'`
  - `DASHSCOPE_API_KEY`: `'sk-ws-H.ERPLDEY.ZTxv.MEUCIQCF9feg-Kzk6N_L9xxfL6UMaY-uP7iUxz_eeW0zAiZEhQIgKSDN9OjmGpMS2_nVR6Sf5GD3xvAbV1_Z_AeyJ2nJep0'`
  - `DASHSCOPE_CHAT_MODEL`: `'qwen3.7-plus'`
- **无伪代码要求**：不得使用 `# TODO`、`pass` 或省略号，必须输出完整可运行的完整 Python 源码。
- **稳健性保障**：`clean_response()` 函数必须具备安全提取逻辑，防止格式微小偏差引发崩溃。

---

## 5. Check & Output Verification (C - 交付与检验要求)
- **零错误直运行**：代码生成后无需任何二次手动修改，通过 `python project3.py` 即可一键直接运行。
- **控制台终端打印格式**：
  必须按如下清晰规范在控制台打印每个句子的预测结果：
  ```text
  sentence 1 --> 【文本内容】
  result --> 【预测类别】
  ```
