# Python 大模型提示词 Day02 填空笔记

## 今日目标

1. 实现大模型调用外部工具。
2. 掌握 zero-shot 和 few-shot 的概念和使用。
3. 掌握 CoT 和 Self-Consistency 的概念和使用。
4. 掌握 ReAct 的原理和代码实现。
5. 掌握提示词常见攻击类型和应对方案。
6. 理解金融项目 Prompt 工程背景。
7. 掌握 Python 调用大模型 API 的基本流程。
8. 理解批处理、流式输出和返回结构调试方法。

## 一、从网页聊天到程序调用大模型

网页聊天是“人问 AI”，API 调用是“________ 替人问 AI，并把答案继续用起来”。

真实项目中，大模型调用的一般流程：

1. 用户在页面输入问题或上传资料。
2. 后台程序读取用户输入。
3. 程序把用户输入、业务规则、参考资料拼成 ________。
4. 程序调用大模型 API。
5. 程序解析模型返回结果。
6. 程序把结果展示给用户、保存到数据库，或者交给下一步流程。

## 二、大模型 API 调用基础

大模型 API 调用，是指 Python 程序通过接口把 ________ 发送给模型服务，再接收模型返回结果的过程。

API 调用四步：

1. ________
2. ________
3. ________
4. ________

关键参数：

| 参数 | 含义 |
|---|---|
| `api_key` | ________ |
| `base_url` | ________ |
| `model` | ________ |
| `messages` | ________ |
| `stream` | ________ |

API 调用的数据流可以理解为：

```text
用户问题 -> Python 程序 -> ________ -> 模型服务 -> response -> 解析结果
```

不会取返回值时，第一步应该 ________，而不是乱猜字段。

## 三、OpenAI 兼容接口、DashScope、Ollama

OpenAI 兼容接口的价值是方便调用不同平台的 ________。

DashScope 是 ________ 提供的大模型调用 SDK。

Ollama 主要用于在 ________ 运行和调用开源大模型。

| 方式 | 核心理解 |
|---|---|
| OpenAI 兼容接口 | ________ |
| DashScope | ________ |
| Ollama | ________ |

换模型名以后，返回字段 ________ 完全一样，所以要先查看真实返回结构。

## 四、批处理与流式输出

批处理是模型生成完整答案后，________ 返回。

流式输出是模型 ________ 返回内容，类似聊天软件逐字显示。

| 对比项 | 批处理 | 流式输出 |
|---|---|---|
| 返回方式 | ________ | ________ |
| 代码处理 | ________ | ________ |
| 常见参数 | 默认方式 | ________ |

流式输出需要使用 ________ 遍历片段。

`end=""` 的作用是 ________。

`flush=True` 的作用是 ________。

流式输出的难点是：不能再用批处理方式直接取 ________，而要从每个 ________ 中取新增文本。

## 五、返回结构调试

模型返回对象里可能包括：

1. 模型回答内容。
2. 角色信息。
3. ________ 消耗。
4. 请求状态。
5. 推理模型的 ________ 内容。

正确排查流程：

1. 打印完整响应对象。
2. 判断它是对象还是 ________。
3. 找到回答文本所在字段。
4. 写解析路径。
5. 换模型后 ________。

比背字段更重要的是：先看 ________，再写取值逻辑。

## 六、Zero-shot

zero-shot 是指不给模型任何 ________，直接让模型完成任务。

zero-shot 适合：

1. 任务 ________。
2. 格式要求不严格。
3. 模型本身容易理解。
4. 对稳定性要求没有特别高。

如果任务要求输出格式严格、判断标准复杂或需要模仿固定风格，就不建议只用 ________。

## 七、Few-shot

few-shot 是指在提示词中给模型少量 ________，让模型模仿示例完成新任务。

few-shot 不是 ________，只是让模型临时参考上下文中的示例。

few-shot 适合：

1. 文本 ________。
2. 信息 ________。
3. 固定格式输出。
4. 风格改写。
5. 金融文本处理。

好的 few-shot 示例应该：

1. 典型。
2. 清晰。
3. 和任务相关。
4. 能体现 ________。
5. 最好包含容易混淆的边界情况。

## 八、CoT 思维链

CoT 的全称是 ________，中文叫 ________。

CoT 的核心是让模型在得到最终答案前，先进行 ________。

Zero-shot CoT 常见写法：

```text
________
```

CoT 适合：

1. 数学题。
2. ________ 题。
3. 多步骤任务。
4. ________ 分析。
5. 任务规划。

CoT 不适合所有任务，因为它会增加 ________ 和 ________。

年龄题中，妹妹年龄的关键不是“现在还是小明的一半”，而是年龄差 ________。

## 九、链式提示

链式提示是把一个复杂任务拆成多个 ________，每一步的结果作为下一步的 ________。

链式提示和 CoT 的区别：

| 对比项 | CoT | 链式提示 |
|---|---|---|
| 重点 | ________ | ________ |
| 形式 | 一次提示中分步 | 多次调用或多阶段处理 |
| 中间结果 | 不一定保存 | 可以保存和检查 |

写摘要任务可以拆成：

1. ________
2. ________
3. ________
4. 检查摘要是否符合字数和格式。

## 十、Self-Consistency

Self-Consistency 是让模型用多条 ________ 生成多个答案，再通过 ________、比较或评审选出更稳定结果的方法。

它适合复杂推理、结果容易波动、需要提高 ________ 的任务。

它的代价是会增加：

1. ________
2. token 成本
3. ________
4. 代码复杂度

工程上不建议使用 ________ 解析模型返回内容，更安全的方式是要求模型输出 JSON，并用 ________ 解析。

## 十一、ReAct

ReAct = ________ + ________。

ReAct 的流程：

1. ________：模型思考下一步要做什么。
2. ________：模型决定调用什么工具。
3. ________：程序执行工具后返回结果。
4. ________：模型根据工具结果给出最终答案。

在 ReAct 中，模型负责 ________，程序负责 ________。

ReAct 的难点是 ________ 控制，因为程序要能读懂模型输出的动作和参数。

模型不会自己真正查数据库或天气，真正执行工具的是 ________。

## 十二、提示词攻击与防御

提示词注入是指用户在输入中加入 ________，试图覆盖原本的系统规则。

越狱攻击是用户试图绕过 ________，让模型生成不合适内容。

数据泄露诱导可能诱导模型输出：

1. API Key。
2. ________。
3. 用户隐私。
4. 公司内部数据。

提示词安全口诀：

1. 用户输入要 ________。
2. 敏感信息不进提示词。
3. 工具调用要 ________。
4. 输出结果要检查。

## 十三、金融项目 Prompt 工程背景

金融场景常见任务：

1. ________
2. ________
3. ________

金融场景不能只要求模型“说得像”，还要要求：

1. 分类准确。
2. 抽取 ________。
3. 格式 ________。
4. 结果可解析。
5. 风险可控。

金融文本分类中，如果模型一会儿输出“新闻”，一会儿输出“新闻报道”，程序就难以处理，所以提示词要明确 ________。

